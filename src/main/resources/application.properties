# Po??czenie do bazy PostgreSQL na Railway
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
spring.datasource.username=postgres
spring.datasource.password=srkLEHANJasCScnXKNOddlykyMktrzaE

# JWT sekrety (PRZENIEŚ DO application.properties lub lepiej ENV)
app.jwt.secret=REPLACE_WITH_STRONG_SECRET_AT_LEAST_32_CHARS_LONG_123456
app.jwt.ttl-seconds=3600

# OpenAPI (jeśli dodasz springdoc)
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Wy??cz H2!
# (Usu? lub zakomentuj poni?sze, je?li by?y)
# spring.h2.console.enabled=true

# Ustawienia produkcyjne (opcjonalnie)
server.port=8080

# Logging (opcjonalnie)
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO

# Railway/Heroku automatycznie ustawi zmienne ?rodowiskowe, mo?esz te? u?y?:
# spring.datasource.url=${DATABASE_URL}