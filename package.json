{
  "name": "drimain-web-mobile",
  "version": "1.0.0",
  "description": "DriMain web and mobile application",
  "private": true,
  "scripts": {
    "gen:api": "openapi -i docs/openapi.yaml -o packages/api-client --useOptions --exportSchemas",
    "lint:api": "swagger-cli validate docs/openapi.yaml"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.27.0",
    "swagger-cli": "^4.0.4"
  }
}