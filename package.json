{
  "name": "drimain-monorepo",
  "version": "0.1.0",
  "description": "Drimain unified web + mobile monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "openapi:generate": "pnpm run -r openapi:generate",
    "build": "pnpm run -r build",
    "test": "pnpm run -r test",
    "lint": "pnpm run -r lint",
    "verify-model-sync": "node tools/verify-model-sync.js"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.8.0",
    "typescript": "^5.2.0",
    "yaml": "^2.3.0"
  },
  "packageManager": "pnpm@8.15.0"
}