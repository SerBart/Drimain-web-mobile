{
  "name": "@drimain/web",
  "version": "1.0.0",
  "private": true,
  "description": "Drimain web application (Spring Boot)",
  "scripts": {
    "dev": "./mvnw spring-boot:run -Dspring-boot.run.profiles=local",
    "build": "./mvnw clean package -DskipTests",
    "test": "./mvnw test",
    "lint": "echo 'TODO: Add Java linting with Checkstyle'",
    "clean": "./mvnw clean"
  },
  "dependencies": {
    "@drimain/core-domain": "workspace:*",
    "@drimain/design-tokens": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}